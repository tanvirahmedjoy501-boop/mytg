app = "monster-trading-bot"  # Change to your preferred unique name

[build]
  dockerfile = "Dockerfile"

[env]
  LOG_LEVEL = "INFO"

[deploy]
  strategy = "immediate"

# Remove the http_service section since this is a worker app (no web server)
